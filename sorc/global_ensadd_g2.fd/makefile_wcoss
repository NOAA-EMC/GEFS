SHELL= 	/bin/bash
INC=     /nwprod/lib/incmod/g2_v2.5.0_4
LIBS=    -L/nwprod/lib -lg2_v2.5.0_4 -lw3nco_v2.0.6_4 -lbacio_v2.0.1_4 \
	 -ljasper -lpng -lz
SRCSf90= ENSADD_G2.f90 printinfr.f90 
SRCS=    $(SRCSf90) 
OBJS=    $(SRCSf:.f=.o) $(SRCSf90:.f90=.o)
CMD=   	global_ensadd_g2 
FC= 	ifort
FFLAGS = -O3 -g -convert big_endian -I $(INC)
LDFLAGS=

# Lines from here on down should not need to be changed.  They are the
# actual rules which make uses to build a.out.
#
all:    $(CMD)

$(CMD):		$(OBJS)
	$(FC) $(LDFLAGS) -o $(@) $(OBJS) $(LIBS)
	rm -f $(OBJS)

$(OBJS):        $(SRCS)
	$(FC) $(FFLAGS) -c $(SRCS)

$(OBJS):        makefile_wcoss

allclean:       clean all

clean:
	-rm -f $(OBJS)

clobber:        clean
	-rm -f $(CMD)
